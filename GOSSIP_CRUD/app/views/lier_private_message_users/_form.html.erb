<%= form_with(model: lier_private_message_user, local: true) do |form| %>
  <% if lier_private_message_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lier_private_message_user.errors.count, "error") %> prohibited this lier_private_message_user from being saved:</h2>

      <ul>
      <% lier_private_message_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
