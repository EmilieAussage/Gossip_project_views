<p id="notice"><%= notice %></p>

<h1>Join Table Tag Gossips</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @join_table_tag_gossips.each do |join_table_tag_gossip| %>
      <tr>
        <td><%= link_to 'Show', join_table_tag_gossip %></td>
        <td><%= link_to 'Edit', edit_join_table_tag_gossip_path(join_table_tag_gossip) %></td>
        <td><%= link_to 'Destroy', join_table_tag_gossip, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Join Table Tag Gossip', new_join_table_tag_gossip_path %>
