<p id="notice"><%= notice %></p>

<h1>Users</h1>

<table>
  <thead>
    <tr>
      <th>Prénom</th>
      <th>Nom</th>
      <th>Description</th>
      <th>Email</th>
      <th>Age</th>
      <th>Ville ID</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.first_name %></td>
        <td><%= user.last_name %></td>
        <td><%= user.description %></td>
        <td><%= user.email %></td>
        <td><%= user.age %></td>
        <td><%= user.city_id %></td>
        <td><a href="<%= user_path(user) %>" class="btn btn-primary">Voir</a></td>
        <td><a href="<%= edit_user_path(user) %>" class="btn btn-primary">Modifier</a></td>      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path %>
